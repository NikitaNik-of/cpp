#include <iostream>

using namespace std;

/*
// Номер 13
int main()
{
    int n;
    float mini = 0;
    float maxi = 0;
    cout << "Input size of array | int >> ";
    cin >> n;

    float a[n];
    for (int i = 0; i < n; ++i){
        cout << "Input array var #" << i+1 << " >> ";
        cin >> a[i];
        if (a[i] > 0 && (mini == 0 || mini > a[i])){
            mini = a[i];
        };
        if (a[i] < 0 && (maxi == 0 || maxi < a[i])){
            maxi = a[i];
        };
    };
    cout << endl << "Max of - | " <<  maxi << endl << "Min of + | " << mini << endl;
}

*/

// Номер 14
auto sum(float a=0, float b){
    return a;
}

int main (){
    cout << sum()
}
